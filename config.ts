// Re-export from the shared config package.
// Import from "@anthyx/config" directly in new code.
export { productConfig } from "@anthyx/config";
export type { ProductConfig } from "@anthyx/config";
